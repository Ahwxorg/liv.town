---
import Layout from '../layouts/Layout.astro';
import Heading from '../components/Heading.astro'
import Paragraph from '../components/Paragraph.astro'
import Greeting from '../components/Greeting.jsx'
import { slide } from 'astro:transitions';

const images = await Astro.glob("../../public/assets/galleryImages/*").then(files => {
  return files.map(file => file.default);
});
---

<Layout title="liv - garden" description="liv to your fullest">
<main>
    <div class="">
      <Heading><Greeting client:load />!</Heading>
      <center><Paragraph>Gallery of images that I've shot</Paragraph></center>
      <center><Paragraph>All of these images are copyrighted to me. <a href="/contact" class="underline underline-offset-1">Contact me</a> for more information.</Paragraph></center>
      <center><Paragraph>I am not going to put watermarks on the images, as that'll ruin their vibe, I think. Please do not use them without explicit permission from me.</Paragraph></center>
      <div class="mt-8 mb-20 gap-8 w-5/6 mx-auto columns-1 sm:columns-2 sm:gap-8 md:columns-3 lg:columns-4 [&>img:not(:first-child)]:mt-8" transition:animate={slide({ duration: '0.4s' })}>
        {
          images.map(({ src }) => (
            <a href={src} target="_blank"><div class="mt-0 m-4 w-full backdrop-blur-xl shadow-2xl p-4"><img src={src} alt="" loading="lazy" transition:animate={slide({ duration: '1.5s' })}></div></a>
          ))
        }
      </div>
    </div>
    <aside transition:name="main-page">
</main>
</Layout>
